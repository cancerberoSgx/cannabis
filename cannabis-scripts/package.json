{
  "name": "extractMemberSignatures",
  "version": "0.0.3",
  "description": "Command line tool to extract given interface member signatures as JSON strings, recursively in the extends chain with the purpose of documentation",
  "main": "dist/src/index.js",
  "bin": "bin/extractMemberSignatures.js",
  "types": "dist/src/index.d.ts",
  "scripts": {
    "test": "npx ts-node -T node_modules/jasmine/bin/jasmine.js",
    "clean": "rm -rf dist docs",
    "build": "npm run clean && npx tsc",
    "prepare": "npm run build",
    "generate-cannabis-functions-and-attributes": "npx tsc && node bin/extractMemberSignatures.js --project ../cannabis/tsconfig.json --target \"**/functionTypes/Functions\" --output cannabisFunctions.json && node bin/extractMemberSignatures.js --project ../cannabis/tsconfig.json --target \"**/attributes/AttributeValues\" --output cannabisAttributes.json",
    "all": "npm run clean && npm run build && npm run test",
    "all-publish": "npm run all && git commit -a -m \"version patch\" && npm version patch && npm publish"
  },
  "keywords": [
    "typescript",
    "ast",
    "query",
    "astq",
    "cli",
    "cannabis"
  ],
  "author": "Sebastián Gurin",
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^12.0.4",
    "ts-node": "^8.2.0",
    "typescript": "^3.5.1"
  },
  "dependencies": {
    "cannabis": "file:../cannabis",
    "ts-morph": "^2.3.0",
    "yargs-parser": "^13.1.0",
    "ts-simple-ast-extra": "^0.1.14"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/cancerberoSgx/cannabis.git"
  },
  "bugs": {
    "url": "https://github.com/cancerberoSgx/cannabis"
  }
}
