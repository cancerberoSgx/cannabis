// import { readFileSync, writeFileSync } from 'fs';
// import { addInterfaceSignatures } from './extractMemberSignatures';

// const f = JSON.parse(readFileSync('dist/test.json').toString())

// writeFileSync('dist/test2.json', JSON.stringify( addInterfaceSignatures(f), null, 2))

// addInterfaceSignatures(f).forEach(f=>console.log(f.signature))