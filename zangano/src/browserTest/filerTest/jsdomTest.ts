
// import test from 'ava'
// import { prepareIndexDb } from './prepareDom';

// prepareIndexDb()

// test('jsdom should be able to rub git clone', async t=>{
//   const git = require('isomorphic-git')
//   // const fs = require('fs')
//   const Filer = require('filer')
//   var fs = new Filer.FileSystem();
//   const files = await git.listFiles({ fs, dir: __dirname })
//   console.log(files)

//   var { ls, cat, cd,exec, mkdir, rm } = new fs.Shell().promises;
//   t.deepEqual(await ls('.'), [])
// })

