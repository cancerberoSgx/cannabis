{
  "name": "typescript-ast-query-editor",
  "version": "0.0.1",
  "description": "TypeScript AST viewer and Advance Query editor",
  "main": "index.js",
  "private": true,
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "clean": "rm -rf dist docs",
    "format": "npx ts-refactor format \"src/**/*.ts*\" \"test/**/*.ts*\" ./formatCodeSettings.json --tsConfigPath ./tsconfig.json --dontAsk",
    "organizeImports": "npx ts-refactor organizeImports \"src/**/*.ts*\" \"test/**/*.ts*\"  ./formatCodeSettings.json --tsConfigPath ./tsconfig.json  --dontAsk ",
    "lint": "npm run organizeImports && npm run format",
    "start": " npx parcel serve  src/index.html -d docs   --no-source-maps",
    "build-dev-no-maps": "export NODE_ENV=development ; parcel build src/index.html -d docs  --no-minify --no-content-hash  --no-source-maps",
    "build-dev": "export NODE_ENV=development ; parcel build src/index.html -d docs  --no-minify --no-content-hash",
    "build": "npm run clean && npx tsc && export NODE_ENV=production && sh build-workers.sh && parcel build src/index.html -d docs --no-content-hash --detailed-report --no-source-maps --public-url './' && rm -rf ../../demos/docs/cannabis/typescript-ast-query-editor/* && mv docs/* ../../demos/docs/cannabis/typescript-ast-query-editor/",
    "all": "npm run clean && npm run lint && npm run build"
  },
  "keywords": [
    "typescript",
    "ast",
    "query",
    "astq"
  ],
  "author": "Sebastián Gurin",
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^12.0.2",
    "@types/react": "^16.8.18",
    "@types/react-dom": "^16.8.4",
    "http-server": "^0.11.1",
    "ts-refactor": "0.0.5",
    "parcel": "^1.12.3",
    "typescript": "^3.4.5",
    "watch": "^1.0.2"
  },
  "dependencies": {
    "cannabis": "..",
    "misc-utils-of-mine-generic": "^0.2.4",
    "monaco-editor": "^0.17.0",
    "react": "^16.8.6",
    "react-dom": "^16.8.6",
    "ts-simple-ast-extra": "^0.1.10"
  }
}
